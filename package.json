{
  "name": "ecoscan",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "dev": "concurrently --raw \"npm run dev --workspace backend\" \"npm run start:lan --workspace mobile\"",
    "typecheck": "npm run typecheck --workspace mobile",
    "seed:ecopoints": "npm run seed:ecopoints --workspace backend",
    "dev:tunnel": "concurrently --raw \"npm run dev --workspace backend\" \"npm run start:tunnel --workspace mobile\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
